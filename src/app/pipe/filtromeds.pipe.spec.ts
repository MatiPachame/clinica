import { FiltromedsPipe } from './filtromeds.pipe';

describe('FiltromedsPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltromedsPipe();
    expect(pipe).toBeTruthy();
  });
});
